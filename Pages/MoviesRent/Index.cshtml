@page
@model RazorPagesMovie.Pages.MoviesRent.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MyMovieRent[0].MyMovieID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MyMovieRent[0].RentDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.MyMovieRent) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MyMovieID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RentDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MyMovieRentID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MyMovieRentID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MyMovieRentID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
